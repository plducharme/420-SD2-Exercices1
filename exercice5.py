import array

arr1 = array.array('d', (4, 7, 9, 14, 2, 9))
arr2 = array.array('d', (45, 3, 6, 23, 8, 12))

arr1.extend(arr2)

# Dans le code précédent, quel sera l’instruction pour ajouter arr2 à arr1 et quel sera l’espace mémoire
# occupé en octets (bytes) par arr1 après l’ajout?
